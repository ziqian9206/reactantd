/* 基本组件
react路由有三种，路由器组件，路径映射组件 导航组件
所有的组件都需要引用 react-router-dom
router 相当于一个外部的父组件，是每个react-router的核心，每一个项目router都提供了<BrowserRouter>
以及<hashRouter>，如果你有从响应到请求的服务器，即服务器动态请求用<BrowserRouter>，如果用的静态文件服务器，那么用<hashRouter>
route，路径到应用程序组件的映射，

*/